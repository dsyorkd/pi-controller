---
# Pi Base Handlers

- name: reload udev
  ansible.builtin.command: udevadm control --reload-rules
  changed_when: true

- name: trigger udev
  ansible.builtin.command: udevadm trigger
  changed_when: true
