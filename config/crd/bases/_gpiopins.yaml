---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: gpiopins.
spec:
  group: ""
  names:
    kind: GPIOPin
    listKind: GPIOPinList
    plural: gpiopins
    singular: gpiopin
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.nodeId
      name: Node
      type: string
    - jsonPath: .spec.pinNumber
      name: Pin
      type: integer
    - jsonPath: .spec.mode
      name: Mode
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.currentValue
      name: Current Value
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: ""
    schema:
      openAPIV3Schema:
        description: GPIOPin is the Schema for the gpiopins API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GPIOPinSpec defines the desired state of GPIOPin
            properties:
              debounceMs:
                description: DebounceMs sets debounce time in milliseconds for input
                  mode
                type: integer
              direction:
                description: Direction specifies the pin direction for input/output
                  modes
                enum:
                - in
                - out
                type: string
              initialValue:
                description: InitialValue sets the initial pin value for output mode
                enum:
                - low
                - high
                type: string
              mode:
                description: Mode specifies the pin mode (input, output, pwm)
                enum:
                - input
                - output
                - pwm
                type: string
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector specifies which node this GPIO pin should
                  be managed on
                type: object
              pinNumber:
                description: PinNumber is the GPIO pin number on the target node
                type: integer
              pullMode:
                description: PullMode configures internal pull resistors for input
                  mode
                enum:
                - none
                - up
                - down
                type: string
              pwmDutyCycle:
                description: PWMDutyCycle sets the PWM duty cycle as percentage (0-100)
                type: number
              pwmFrequency:
                description: PWMFrequency sets the PWM frequency in Hz (PWM mode only)
                type: integer
            required:
            - mode
            - nodeSelector
            - pinNumber
            type: object
          status:
            description: GPIOPinStatus defines the observed state of GPIOPin
            properties:
              actualDutyCycle:
                description: ActualDutyCycle is the currently configured PWM duty
                  cycle
                type: number
              actualFrequency:
                description: ActualFrequency is the currently configured PWM frequency
                type: integer
              actualMode:
                description: ActualMode is the currently configured pin mode
                enum:
                - input
                - output
                - pwm
                type: string
              conditions:
                description: Conditions represent the current service state
                items:
                  description: GPIOCondition represents a condition of the GPIO pin
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details
                      type: string
                    reason:
                      description: Reason is the reason for the condition's last transition
                      type: string
                    status:
                      description: Status of the condition (True, False, Unknown)
                      type: string
                    type:
                      description: Type of condition
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              currentValue:
                description: CurrentValue is the current pin state (high/low)
                enum:
                - low
                - high
                type: string
              lastUpdated:
                description: LastUpdated is the timestamp of the last status update
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              nodeId:
                description: NodeID is the ID of the node managing this pin
                type: string
              phase:
                description: Phase represents the current lifecycle phase
                enum:
                - Pending
                - Configuring
                - Ready
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
